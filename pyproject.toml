[project]
name = "wiki"
version = "0.1.0"
description = "WJP Wiki — shared workflows, tools, and best practices."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.5.0",
]

[dependency-groups]
dev = [
    "commitizen>=4.16.2",
]
